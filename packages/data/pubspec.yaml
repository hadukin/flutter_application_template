name: data
description: "A new Flutter package project."
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  injectable: ^2.7.1+4
  network:
    path: ../network
  domain:
    path: ../domain
  di:
    path: ../di
  storage:
    path: ../storage
  use_case:
    path: ../use_case

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  injectable_generator: ^2.12.0
